import React from 'react';

const Login = () => {
    return (
        <div>
            <div>Login</div>
            <button onClick={() => {alert('click')}}>
                Login
            </button>
        </div>
    )
}
export default Login